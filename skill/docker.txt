Docker is by no means de facto standard now for local
development. But I also use it with success for deployments
(mostly in my own projects). I have few open sources with
its docker images available publically.